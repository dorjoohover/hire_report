version: "3.9"
services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    networks:
      - app-network

  app:
    build: .
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
